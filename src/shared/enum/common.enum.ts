export enum NodeEnv {
  Development = 'dev',
  STAGING = 'staging',
  UAT = 'uat',
  Production = 'production',
}

export enum SortEnum {
  ASC = 'ASC',
  DESC = 'DESC',
}

export enum UserRole {
  ADMIN = 'admin',
  USER = 'user',
}
